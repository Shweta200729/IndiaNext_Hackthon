# Core backend
fastapi>=0.111.0,<0.116.0
uvicorn[standard]==0.34.0
python-dotenv==1.2.1
python-multipart
pydantic[email]>=2.0.0,<3.0.0
pydantic-settings>=2.0.0
groq>=0.13.0

# Database & Storage
supabase==2.28.0
bcrypt

# Testing
pytest
pytest-asyncio

# NOTE: Heavy ML dependencies (torch, langchain, mlflow, chromadb, etc.)
# are in requirements-ml.txt — install those separately for local ML features.
# Render deploys using ONLY this file for a lightweight, fast build.
# ── Heavy ML Dependencies ─────────────────────────────────────────────────────
# Install AFTER requirements.txt for local development with full ML features.
# Usage: pip install -r requirements-ml.txt
#
# These are NOT installed on Render (free tier cannot handle the size).
# On Render, ML endpoints (/fl/simulate, /fl/model, /fl/update) will return
# 503 with a helpful message. All auth + dashboard API endpoints still work.
# ─────────────────────────────────────────────────────────────────────────────

# Federated Learning
torch>=2.0.0
torchvision

# RAG Chatbot
langchain>=0.3.0
langchain-community>=0.3.0
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Dataset Discovery
kaggle>=1.6.0

# Experiment Tracking
mlflow>=2.13.0
